# Welcome to TowLink 👋

This is an [Expo](https://expo.dev) project created with [`create-expo-app`](https://www.npmjs.com/package/create-expo-app).

## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Fill out .env.example, then rename it to just .env

3. Run

   ```bash
   npx expo start
   ```
