---
phase: 01-companies-admin
plan: 07
type: execute
wave: 5
depends_on:
  - 01-05
  - 01-06
files_modified: []
autonomous: false
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - AUTH-01

must_haves:
  truths:
    - "Admin seeded in Firestore routes to (admin) route group on login"
    - "Admin with no companyId is redirected to company-setup screen; filling in name/address/radius and submitting creates a Firestore company document with geocoded location and geohash"
    - "After company registration, admin lands on the Jobs/Drivers dashboard (companyId now set in AuthContext)"
    - "Driver with authorized email signs up and user doc has role='driver' and correct companyId"
    - "Driver with unauthorized email sees error: 'This email isn't registered with a tow yard. Contact your company admin.'"
    - "Admin Drivers tab shows all company drivers; active drivers show 'Offline' chip; deactivated drivers show 'Deactivated' chip at half opacity"
    - "Swiping left on a driver row reveals Deactivate button; tapping it shows confirmation and sets isActive=false"
    - "Admin Jobs tab shows active jobs updating in real-time when Firestore trip docs change"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 1 requirements. Admin seeds, company registration flow, driver signup flows, dashboard real-time updates, and deactivation gesture must all work end-to-end.

Purpose: No automated test infrastructure exists in this project (TEST-01 Maestro is Phase 5). Human verification is the phase gate — all 6 manual flows must pass before `/gsd:verify-work` is called.

Output: Verified working Phase 1 implementation. Manual sign-off on each flow.
</objective>

<execution_context>
@/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-companies-admin/01-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Phase 1 end-to-end flows — admin routing, company registration, driver signup, deactivation, real-time dashboard</name>
  <action>Run `npx expo start`, open on iOS/Android simulator, and complete all 6 verification flows described in how-to-verify.</action>
  <files>none — verification only, no files modified</files>
  <verify>
    <automated>cd /Users/chris/projects/towlink && npm run lint -- --quiet 2>&1 | head -5</automated>
  </verify>
  <done>All 6 flows pass: admin routing works, company registration creates Firestore doc with geocoded location, authorized driver signup links to company, unauthorized signup blocked, deactivation sets isActive=false, Jobs tab updates in real-time.</done>

  <what-built>
    Full Phase 1 implementation:
    - types/models.ts extended with Company, admin role, companyId, isActive
    - services/firebase/companies.ts with createCompany, addAuthorizedEmail, findCompanyByEmail, deactivateDriver, listenToCompanyJobs, listenToCompanyDrivers
    - AuthContext extended with admin role and companyId
    - app/_layout.tsx routes admin users to /(admin)
    - app/(admin)/company-setup.tsx — in-app company registration screen
    - signUpDriverWithEmail() pre-checks authorizedEmails before account creation
    - app/(admin)/index.tsx — Jobs tab with real-time FlatList and status badges
    - app/(admin)/drivers.tsx — Drivers tab with swipe-to-deactivate and Add Driver sheet
  </what-built>

  <how-to-verify>
    Run `npx expo start` and open on iOS/Android simulator. Complete each verification flow:

    **Flow 1 — Admin routing (AUTH-01, COMP-04)**
    Setup: In Firebase console → Firestore → `users` collection, find or create a test user doc. Set fields: `role: "admin"`, `companyId: null` (or omit companyId — simulates fresh admin with no company yet).
    Steps:
    1. Sign in with that user's email/password
    2. Expected: redirected to admin dashboard route group (/(admin))
    3. Because companyId is null, expected: redirected to company-setup screen (NOT to commuter or driver screen)

    **Flow 2 — Company registration (COMP-01)**
    Steps:
    1. On the company-setup screen (reached from Flow 1), fill in:
       - Company Name: "Test Tow & Recovery"
       - Address: any real street address (e.g. "1 Apple Park Way, Cupertino, CA 95014")
       - Service Radius: 25
    2. Tap "Register Company"
    3. Expected: loading indicator while geocoding + Firestore write runs
    4. Expected: after success, redirected to the Jobs/Drivers dashboard tabs
    5. In Firebase console → Firestore → `companies` collection: verify the new document contains all required fields: name, address, location {latitude, longitude}, geohash, serviceRadiusKm, authorizedEmails: [], ownerUid, createdAt
    6. Expected: geohash field is a non-empty string (geohashForLocation result)

    **Flow 3 — Add Driver and authorized signup (COMP-02, AUTH-01)**
    Steps:
    1. Log in as admin → Drivers tab → tap "+" button → Add Driver sheet opens
    2. Enter a new test email (e.g. driver-test@testcompany.com) → tap "Add Driver"
    3. Expected: success banner "Driver added. They can now sign up with this email."
    4. Sign out; attempt to sign up with that email via signup screen
    5. Expected: account created; user doc in Firestore has role="driver" and companyId set

    **Flow 4 — Unauthorized driver blocked (AUTH-01)**
    Steps:
    1. Attempt signup with an email NOT in any company's authorizedEmails
    2. Expected: signup fails with error "This email isn't registered with a tow yard. Contact your company admin."
    3. Confirm NO new auth account was created in Firebase console (Auth → Users)

    **Flow 5 — Swipe-to-deactivate (COMP-03)**
    Steps:
    1. Open admin Drivers tab (must have at least 1 driver in the company)
    2. Swipe left on a driver row
    3. Expected: red "Deactivate Driver" action cell revealed
    4. Tap "Deactivate Driver" → confirmation Alert with "Deactivate Driver" title, "This driver will no longer receive job assignments.", "Keep Driver" cancel, "Deactivate Driver" confirm
    5. Tap "Deactivate Driver" confirm
    6. Expected: driver row dims to opacity 0.5, chip changes to "Deactivated"
    7. Check Firestore console: `users/{driverUid}.isActive` is `false`

    **Flow 6 — Real-time Jobs tab (COMP-04)**
    Steps:
    1. Open admin dashboard Jobs tab
    2. From Firebase console, add a document to `trips` collection: companyId="your-test-companyId", status="en_route", commuterId="testuser", driverId="testdriver", startTime=(now timestamp)
    3. Expected: new job row appears on Jobs tab WITHOUT refreshing
    4. Change the document's status to "arrived" in console
    5. Expected: badge updates from "En Route" to "Arrived" without refresh
  </how-to-verify>

  <resume-signal>
    Test each flow and report results. Type "approved" if all 6 flows pass. If any flow fails, describe what happened and which step it failed at — executor will fix and re-verify.
  </resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 6 flows. All flows from VALIDATION.md Manual-Only Verifications table must pass.
</verification>

<success_criteria>
All 5 roadmap success criteria for Phase 1 are TRUE:
1. Admin can register a new tow yard company using the in-app company-setup screen; company persists to Firestore with geocoded location
2. Admin can add driver email; that driver can sign up and is linked to the company
3. Admin can deactivate a driver (isActive=false + visual change)
4. Jobs tab updates in real-time when Firestore trip docs change
5. Unauthorized driver email is blocked with correct error message
</success_criteria>

<output>
After completion, create `.planning/phases/01-companies-admin/01-07-SUMMARY.md`
</output>
