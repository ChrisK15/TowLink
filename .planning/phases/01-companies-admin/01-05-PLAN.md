---
phase: 01-companies-admin
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - app/(admin)/index.tsx
autonomous: true
requirements:
  - COMP-04

must_haves:
  truths:
    - "Admin Jobs tab renders a FlatList of active trip rows"
    - "Each job row shows a status badge chip (colored pill), commuter name, service type, and assigned driver name or 'Unassigned'"
    - "While jobs are loading, an ActivityIndicator is shown centered on screen"
    - "When there are no active jobs, an empty state view shows heading 'No active jobs' and body text 'Jobs will appear here when commuters submit requests.'"
    - "The status badge chip uses the correct background/text color pair for each status value per UI-SPEC"
    - "The FlatList is a real-time listener that updates without user action when Firestore changes"
  artifacts:
    - path: "app/(admin)/index.tsx"
      provides: "Full admin Jobs tab screen"
      contains: "FlatList"
  key_links:
    - from: "app/(admin)/index.tsx"
      to: "hooks/use-company-jobs.ts"
      via: "useCompanyJobs(companyId) real-time data"
      pattern: "useCompanyJobs"
    - from: "app/(admin)/index.tsx"
      to: "context/auth-context.tsx"
      via: "useAuth() for companyId"
      pattern: "useAuth"
---

<objective>
Build the Admin Jobs tab screen: a real-time FlatList of active jobs with status badges, commuter info, and assigned driver names.

Purpose: This implements COMP-04 — admin can view all active jobs and their statuses in real-time. The screen uses the hook and data layer built in earlier plans; this plan is purely the UI layer.

Output:
- `app/(admin)/index.tsx` — full Jobs tab replacing the stub from Plan 04
</objective>

<execution_context>
@/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-companies-admin/01-UI-SPEC.md
@.planning/phases/01-companies-admin/01-CONTEXT.md
@.planning/phases/01-companies-admin/01-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor builds against -->

From hooks/use-company-jobs.ts (after Plan 04):
```typescript
export function useCompanyJobs(companyId: string | null): {
  jobs: Trip[];
  loading: boolean;
  error: string | null;
}
```

From types/models.ts (after Plan 01):
```typescript
export interface Trip {
  id: string;
  requestId: string;
  commuterId: string;
  driverId: string;
  status: 'en_route' | 'arrived' | 'in_progress' | 'completed' | 'cancelled';
  companyId?: string;
  pickupAddress: string;
  dropoffAddress: string;
  // ... other fields
}
// Note: Trip does not have commuterName, driverName, or serviceType.
// For Phase 1, display commuterId truncated or "Commuter" as placeholder;
// full commuter name resolution is a Phase 2/3 enhancement.
```

From context/auth-context.tsx (after Plan 02):
```typescript
export function useAuth(): { user, role, companyId: string | null, loading, signOut, refreshRole }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build full Admin Jobs tab screen</name>
  <files>app/(admin)/index.tsx</files>
  <read_first>
    - app/(admin)/index.tsx (current stub state from Plan 04)
    - .planning/phases/01-companies-admin/01-UI-SPEC.md (full UI spec: Job row pattern, Status Badges table, Loading States, Copywriting Contract, Color table)
    - types/models.ts (Trip interface — confirm available fields)
    - hooks/use-company-jobs.ts (return shape)
    - context/auth-context.tsx (verify companyId exposed on useAuth)
  </read_first>
  <action>
    Replace the stub in `app/(admin)/index.tsx` with the full Jobs tab screen.

    **Design requirements from UI-SPEC (implement exactly):**

    Status badge color map (pill: borderRadius 12, paddingHorizontal 8, paddingVertical 4):
    - en_route: bg `#F3E5F5`, text `#6A1B9A`, label "En Route"
    - arrived: bg `#E8F5E9`, text `#2E7D32`, label "Arrived"
    - in_progress: bg `#E8F5E9`, text `#2E7D32`, label "In Progress"
    - completed: bg `#F5F5F5`, text `#666666`, label "Completed"
    - cancelled: bg `#FFEBEE`, text `#C62828`, label "Cancelled"

    Job row layout (minHeight 64, separator 1px `#E0E0E0`):
    - Left: StatusBadge chip (primary focal point)
    - Middle: commuter identifier (Label: 16px, semibold `#000`) + address/service type (Body: 14px, regular `#666666`)
    - Right: driver identifier or "Unassigned" (Body: 14px, regular `#999999` for unassigned)

    Data available on Trip: `status`, `commuterId`, `driverId`, `pickupAddress`, `dropoffAddress`.
    Since Trip has no commuterName or serviceType fields yet (Phase 2 adds these via dispatch):
    - Show `pickupAddress` as the secondary text in the middle column
    - Show `commuterId` truncated to 8 chars as the primary identifier (e.g. "User abc12345")
    - Show `driverId` truncated to 8 chars as the right column (e.g. "Driver abc12345"), or "Unassigned" if driverId is null/empty

    Loading state: full-screen centered `ActivityIndicator` size="large" color="#007AFF"

    Empty state (no jobs):
    - Centered view with heading "No active jobs" (20px, semibold `#000`)
    - Body "Jobs will appear here when commuters submit requests." (14px, regular `#666666`)

    Screen background: `#F5F5F5`. List background: `#F5F5F5`. Card face: `#FFFFFF`.

    Use `FlatList` with `keyExtractor={(item) => item.id}`. Each row is a `View` with `backgroundColor: '#FFFFFF'`, `paddingHorizontal: 16`, `paddingVertical: 12`, `minHeight: 64`, border-bottom `1px #E0E0E0`.

    The screen title should be shown in a simple header view with text "Jobs" (20px, semibold `#000`, paddingHorizontal 16, paddingTop 16, paddingBottom 8).

    Do not add any navigation back buttons or tab bar — `headerShown: false` is set in the layout.
  </action>
  <verify>
    <automated>cd /Users/chris/projects/towlink && npm run lint -- --quiet 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/(admin)/index.tsx contains `FlatList`
    - app/(admin)/index.tsx contains `useCompanyJobs`
    - app/(admin)/index.tsx contains `ActivityIndicator`
    - app/(admin)/index.tsx contains the string `"No active jobs"`
    - app/(admin)/index.tsx contains the string `"Jobs will appear here when commuters submit requests."`
    - app/(admin)/index.tsx contains `#F3E5F5` (en_route badge color)
    - app/(admin)/index.tsx contains `#E8F5E9` (arrived/in_progress badge color)
    - app/(admin)/index.tsx contains `borderRadius: 12` (badge pill shape)
    - npm run lint exits with code 0
  </acceptance_criteria>
  <done>Jobs tab renders FlatList with status badges, job rows, loading state, and empty state; lint passes.</done>
</task>

</tasks>

<verification>
After task completion:
1. `npm run lint` exits 0
2. `grep -n "FlatList\|useCompanyJobs\|No active jobs" app/(admin)/index.tsx` returns matches for all three
3. `grep -n "#F3E5F5\|#E8F5E9\|borderRadius: 12" app/(admin)/index.tsx` returns matches (status badge colors)
</verification>

<success_criteria>
- Jobs tab shows ActivityIndicator while loading
- Jobs tab shows "No active jobs" empty state when list is empty
- Each job row shows: status badge chip (color-coded per UI-SPEC) + commuter identifier + pickup address + driver identifier or "Unassigned"
- FlatList is powered by useCompanyJobs real-time hook
- `npm run lint` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-companies-admin/01-05-SUMMARY.md`
</output>
