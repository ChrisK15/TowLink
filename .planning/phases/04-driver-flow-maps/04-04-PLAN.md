---
phase: 04-driver-flow-maps
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - scripts/seed-emulator.js
autonomous: false
requirements: [DRVR-01, DRVR-02, DRVR-03, DRVR-04, MAP-01, MAP-02, MAP-03]

must_haves:
  truths:
    - "Driver can accept a seeded request and see InstructionCard with route directions"
    - "Driver can advance trip through en_route -> arrived -> in_progress -> completed"
    - "Driver can cancel during en_route and commuter sees FindingDriverModal again"
    - "Commuter sees driver marker on map moving during active trip"
    - "Commuter sees blue route polyline and live ETA in trip sheet"
    - "Location permission denial shows Alert with Open Settings button"
  artifacts:
    - path: "scripts/seed-emulator.js"
      provides: "Seed data includes en_route trip for commuter-side testing"
      contains: "en_route"
  key_links:
    - from: "scripts/seed-emulator.js"
      to: "trips collection"
      via: "seed document with status en_route"
      pattern: "status.*en_route"
---

<objective>
Extend emulator seed data to include an active en_route trip for commuter-side testing, then perform human verification of all Phase 4 success criteria across both driver and commuter flows.

Purpose: Validates all 7 requirements (DRVR-01 through DRVR-04, MAP-01 through MAP-03) are working end-to-end with real emulator data.
Output: Updated seed script, human verification of all flows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-driver-flow-maps/04-CONTEXT.md
@.planning/phases/04-driver-flow-maps/04-02-SUMMARY.md
@.planning/phases/04-driver-flow-maps/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add en_route trip to emulator seed data</name>
  <files>scripts/seed-emulator.js</files>
  <read_first>
    - scripts/seed-emulator.js (full file -- existing seed patterns, test user UIDs, existing request/trip data structures)
    - types/models.ts (Trip interface fields)
    - .planning/phases/04-driver-flow-maps/04-RESEARCH.md (Wave 0 Gaps section: seed request with status 'en_route')
  </read_first>
  <action>
Add a new seeded trip document to the seed script so the commuter screen can be tested without manually accepting a request first.

Find the existing seed data section that creates the `test-request-claimed` request. After it, add a new trip document:

```javascript
// Active en_route trip for commuter-side Phase 4 testing
await db.collection('trips').doc('test-trip-enroute').set({
  requestId: 'test-request-claimed',
  commuterId: COMMUTER_UID,    // use whatever constant the seed uses for commuter
  driverId: DRIVER_UID,        // use whatever constant the seed uses for driver
  status: 'en_route',
  pickupLocation: { latitude: 34.0522, longitude: -118.2437 },  // match existing seed pickup
  dropoffLocation: { latitude: 34.0625, longitude: -118.2410 }, // match existing seed dropoff
  pickupAddress: '200 N Spring St, Los Angeles, CA',  // match existing seed address or use similar
  dropoffAddress: '100 N Grand Ave, Los Angeles, CA',
  startTime: admin.firestore.Timestamp.now(),
  distance: 1.2,
  estimatedPrice: 45,
  driverPath: [],
  companyId: COMPANY_ID,       // use whatever constant the seed uses for company
});
```

Use the EXACT same UID constants and address patterns the existing seed script uses. Read the file first to find these values -- do NOT guess them. The trip must reference `test-request-claimed` as its requestId so the cancel flow has a valid request to reset.

Also ensure the existing `test-request-claimed` request has `claimedByDriverId` set to the driver UID (it likely already does based on Phase 3 decisions) so the cancel flow can properly clear it.
  </action>
  <verify>
    <automated>cd /Users/chris/projects/towlink && grep -c "en_route" scripts/seed-emulator.js && grep "test-trip-enroute" scripts/seed-emulator.js</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed-emulator.js contains a trip document with id 'test-trip-enroute'
    - Trip document has status 'en_route'
    - Trip document has valid commuterId, driverId, and requestId matching existing seed constants
    - Trip document has pickupLocation and dropoffLocation with valid LA-area coordinates
    - Trip document has all required fields: startTime, distance, estimatedPrice, driverPath, companyId
  </acceptance_criteria>
  <done>Seed script creates an active en_route trip enabling commuter-side testing without manual request/accept flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify all Phase 4 flows</name>
  <files>n/a</files>
  <action>Present the verification checklist to the user and wait for approval. All implementation is complete from Plans 01-03. This task is manual testing only.</action>
  <what-built>
Complete Phase 4 driver flow and maps implementation:
1. Google Directions API integration with route polylines and instruction cards
2. Live driver location tracking (5-second updates to Firestore)
3. Driver InstructionCard overlay with GPS-to-step matching
4. Cancel Job button with re-dispatch flow
5. Open in Maps button for external navigation
6. Commuter DriverMarker with smooth animation
7. Commuter route polyline (blue) with stage-based switching
8. Live ETA replacing hardcoded "8 min away"
9. Improved location permission handling on both screens
  </what-built>
  <how-to-verify>
**Setup:**
1. Start emulators: `npm run emulators` (in separate terminal)
2. Seed data: `npm run emulators:seed`
3. Start app: `npx expo start`

**IMPORTANT PREREQUISITE:** Before testing, verify the Directions API is enabled in Google Cloud Console:
- Go to Cloud Console -> APIs and Services -> Enabled APIs
- Search for "Directions API" -- if not listed, enable it
- Without this, route polylines and ETA will not work (fetchDirections returns null)

**Test 1: DRVR-01 -- Accept/Decline (already working, quick verify)**
- Login as driver (use seed driver credentials)
- Go online, wait for seeded claimed request popup
- Verify RequestPopup appears with Accept/Decline buttons
- Tap Accept

**Test 2: DRVR-02 -- Map Directions During Active Trip**
- After accepting: verify InstructionCard appears at top of map with route instruction
- If InstructionCard shows "Calculating route..." initially, wait a moment for Directions API
- Verify green route polyline (#34C759) renders on MapView from driver to pickup
- Verify "Open in Maps" button appears in trip sheet near pickup address

**Test 3: DRVR-03 -- Trip Status Progression**
- Tap "I've Arrived" -- verify status changes, InstructionCard updates
- Tap "Start Service" -- verify status changes
- Tap "Complete Trip" -- verify trip completes, driver returns to idle state

**Test 4: DRVR-04 -- Cancel During En Route**
- Accept another request (or re-seed)
- While in en_route status, look for red "Cancel Job" button in trip sheet
- Tap it -- verify Alert says "Cancel Job?" / "This job will be reassigned to another driver."
- Tap "Yes, Cancel Job"
- Verify driver returns to idle state

**Test 5: MAP-01 -- Driver Marker on Commuter Map**
- Login as commuter (use seed commuter credentials, separate simulator or device)
- With the seeded en_route trip active, verify green circle marker with car icon appears on map
- Verify marker position updates as driver moves (may need GPX simulation on iOS Simulator)

**Test 6: MAP-02 -- Route Polyline and ETA**
- On commuter screen with active trip: verify blue polyline (#1565C0) renders
- Verify CommuterTripSheet shows ETA like "12 mins" instead of "8 min away"
- If ETA shows "-- min away", Directions API may not be enabled (see prerequisite)

**Test 7: MAP-03 -- Location Permission Denial**
- On iOS Simulator: Settings -> Privacy -> Location Services -> TowLink -> set to "Never"
- Reopen app
- Verify Alert appears with title "Location Required" and "Open Settings" button
- Verify app does not crash
  </how-to-verify>
  <verify>
    <automated>MISSING -- checkpoint task requires manual human verification</automated>
  </verify>
  <done>All 7 Phase 4 requirements verified by human tester with Firebase emulators.</done>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 6 Phase 4 success criteria from ROADMAP.md verified:
1. Driver receives assigned job and can accept/decline (DRVR-01)
2. Driver sees map directions to pickup (DRVR-02)
3. Driver advances trip through all stages with real-time commuter updates (DRVR-03)
4. Driver can cancel during en_route, job returns to dispatch (DRVR-04)
5. Commuter sees driver live location + route polyline + ETA (MAP-01, MAP-02)
6. Location permissions handled gracefully (MAP-03)
</verification>

<success_criteria>
All 7 requirements verified manually with Firebase emulators. Phase 4 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-driver-flow-maps/04-04-SUMMARY.md`
</output>
