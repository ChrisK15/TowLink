---
phase: 03-maestro-e2e-testing
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - "app/(auth)/login.tsx"
  - "app/(auth)/signup.tsx"
  - "app/(commuter)/index.tsx"
  - "app/(driver)/index.tsx"
  - "app/(admin)/index.tsx"
  - "app/(admin)/drivers.tsx"
  - components/RequestServiceSheet.tsx
  - components/FindingDriverModal.tsx
  - components/ActiveTripSheet.tsx
  - components/RequestPopup.tsx
  - .maestro/subflows/login-commuter.yaml
  - .maestro/subflows/login-driver.yaml
  - .maestro/subflows/login-admin.yaml
  - .maestro/commuter-happy-path.yaml
  - .maestro/commuter-no-drivers.yaml
  - .maestro/driver-accept-complete.yaml
  - .maestro/driver-decline.yaml
  - .maestro/admin-basic.yaml
  - scripts/run-e2e.sh
autonomous: false
requirements:
  - TEST-01
must_haves:
  truths:
    - "Maestro can find and interact with login form elements using testID selectors"
    - "Commuter happy-path flow test passes end-to-end against emulator"
    - "Driver accept-and-complete flow test passes end-to-end against emulator"
    - "Admin basic navigation test passes against emulator"
    - "All tests can be run with a single command"
  artifacts:
    - path: ".maestro/commuter-happy-path.yaml"
      provides: "Commuter request submission E2E test"
      contains: "appId: com.towlink.app"
    - path: ".maestro/driver-accept-complete.yaml"
      provides: "Driver accept and complete trip E2E test"
      contains: "appId: com.towlink.app"
    - path: ".maestro/admin-basic.yaml"
      provides: "Admin login and tab navigation test"
      contains: "appId: com.towlink.app"
    - path: ".maestro/subflows/login-commuter.yaml"
      provides: "Reusable commuter login subflow"
      contains: "test-commuter@test.com"
    - path: "scripts/run-e2e.sh"
      provides: "Single-command test runner"
      contains: "maestro test"
  key_links:
    - from: ".maestro/commuter-happy-path.yaml"
      to: "app/(auth)/login.tsx"
      via: "testID selectors on email/password inputs"
      pattern: "id: .email-input."
    - from: ".maestro/driver-accept-complete.yaml"
      to: "components/ActiveTripSheet.tsx"
      via: "testID on advance-trip-status button"
      pattern: "id: .advance-trip-btn."
    - from: ".maestro/subflows/login-commuter.yaml"
      to: "scripts/seed-emulator.js"
      via: "Matching test credentials"
      pattern: "test-commuter@test.com"
---

<objective>
Add testID props to all screens used in Maestro flows, create all Maestro YAML test flows (commuter, driver, admin), and create a single-command test runner script. Then verify the full suite works on the iOS Simulator dev build.

Purpose: These are the actual E2E test flows that exercise the commuter request, driver dispatch, and admin dashboard workflows -- the core deliverable of Phase 3.

Output: testIDs on interactive elements, 5 Maestro test flows + 3 login subflows, a shell script runner, and verified passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-maestro-e2e-testing/03-CONTEXT.md
@.planning/phases/03-maestro-e2e-testing/03-RESEARCH.md
@.planning/phases/03-maestro-e2e-testing/03-01-SUMMARY.md

<interfaces>
<!-- Existing screen text labels that Maestro flows will target -->

From app/(auth)/login.tsx:
- Email TextInput: placeholder="Email" (needs testID="email-input")
- Password TextInput: placeholder="Password" (needs testID="password-input")
- Login Pressable: text="Login" (needs testID="login-btn")
- Sign Up link: text="Don't have an account? Sign Up"

From app/(auth)/signup.tsx:
- Email TextInput: placeholder="Email" (needs testID="signup-email-input")
- Password TextInput: placeholder="Password" (needs testID="signup-password-input")
- Confirm Password TextInput: placeholder="Confirm Password" (needs testID="signup-confirm-password-input")
- Sign Up Pressable: text="Sign Up" (needs testID="signup-btn")

From app/(commuter)/index.tsx:
- Request button: text="Request Roadside Assistance" (needs testID="request-assistance-btn")
- Sign Out button: text="Sign Out"

From components/RequestServiceSheet.tsx:
- Towing card: text="Towing" (needs testID="service-tow")
- Detect My Location button: text="Detect My Location" (needs testID="detect-location-btn")
- Pickup TextInput: placeholder="Enter pickup address" (needs testID="pickup-address-input")
- Dropoff TextInput: placeholder="Enter destination address" (needs testID="dropoff-address-input")
- Year TextInput: placeholder="Year" (needs testID="vehicle-year-input")
- Make TextInput: placeholder="Make" (needs testID="vehicle-make-input")
- Model TextInput: placeholder="Model" (needs testID="vehicle-model-input")
- Submit button: text="Request Service Now" (needs testID="submit-request-btn")

From components/FindingDriverModal.tsx:
- "Searching for a driver" text (visible when status=searching)
- "No drivers available" text (visible when status=no_drivers)
- "Try Again" button (visible when no_drivers)
- "Cancel" button

From app/(driver)/index.tsx:
- Online/Offline text: "Online" / "Offline"
- Go Online button: text="Go Online" (needs testID="go-online-btn")
- Switch toggle for online status

From components/RequestPopup.tsx:
- "New Request" title
- Accept button: text="Accept Request" (needs testID="accept-request-btn")
- Decline button: text="Decline" (needs testID="decline-request-btn")

From components/ActiveTripSheet.tsx:
- Status labels: "En Route to Pickup", "Arrived at Pickup", "Service in Progress", "Trip Completed"
- Action buttons: "I've Arrived", "Start Service", "Complete Trip" (needs testID="advance-trip-btn")

From app/(admin)/index.tsx:
- Header: text="Jobs"
- Sign Out: text="Sign Out"
- Empty state: text="No active jobs"

From app/(admin)/drivers.tsx:
- Tab label: "Drivers" (visible in tab bar)

From app/(admin)/_layout.tsx:
- Tab: title="Jobs"
- Tab: title="Drivers"

Seed data credentials (from 03-01 seed script):
- test-commuter@test.com / password123
- test-driver@test.com / password123
- test-admin@test.com / password123
- test-driver2@test.com / password123

Seed data contract (from 03-01 seed script -- CRITICAL for driver flows):
- Request document `test-request-claimed` uses `status: 'claimed'` and `claimedByDriverId: <test-driver-uid>`
- This matches the `listenForClaimedRequests` hook which queries `where('status', '==', 'claimed')` and `where('claimedByDriverId', '==', driverId)`
- The RequestPopup will appear when the driver goes online and the hook picks up this request
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testID props to all screens and components used in Maestro flows</name>
  <files>
    app/(auth)/login.tsx,
    app/(auth)/signup.tsx,
    app/(commuter)/index.tsx,
    app/(driver)/index.tsx,
    components/RequestServiceSheet.tsx,
    components/FindingDriverModal.tsx,
    components/ActiveTripSheet.tsx,
    components/RequestPopup.tsx
  </files>
  <read_first>
    - "app/(auth)/login.tsx"
    - "app/(auth)/signup.tsx"
    - "app/(commuter)/index.tsx"
    - "app/(driver)/index.tsx"
    - components/RequestServiceSheet.tsx
    - components/FindingDriverModal.tsx
    - components/ActiveTripSheet.tsx
    - components/RequestPopup.tsx
  </read_first>
  <action>
Add `testID` props to interactive elements on each screen. Maestro maps React Native `testID` to `id` selectors.

**app/(auth)/login.tsx:**
- Email TextInput (line ~46): add `testID="email-input"`
- Password TextInput (line ~54): add `testID="password-input"`
- Login Pressable (line ~64): add `testID="login-btn"`

**app/(auth)/signup.tsx:**
- Email TextInput (line ~76): add `testID="signup-email-input"`
- Password TextInput (line ~85): add `testID="signup-password-input"`
- Confirm Password TextInput (line ~94): add `testID="signup-confirm-password-input"`
- Sign Up Pressable (line ~103): add `testID="signup-btn"`

**app/(commuter)/index.tsx:**
- "Request Roadside Assistance" TouchableOpacity (line ~103): add `testID="request-assistance-btn"`

**app/(driver)/index.tsx:**
- "Go Online" TouchableOpacity (line ~367): add `testID="go-online-btn"`
- Online/Offline Switch (line ~311): add `testID="online-toggle"`

**components/RequestServiceSheet.tsx:**
- Pickup address TextInput (line ~352): add `testID="pickup-address-input"`
- Dropoff address TextInput (line ~370): add `testID="dropoff-address-input"`
- "Detect My Location" TouchableOpacity (line ~338): add `testID="detect-location-btn"`
- Vehicle Year TextInput (line ~384): add `testID="vehicle-year-input"`
- Vehicle Make TextInput (line ~395): add `testID="vehicle-make-input"`
- Vehicle Model TextInput (line ~408): add `testID="vehicle-model-input"`
- "Request Service Now" TouchableOpacity (line ~477): add `testID="submit-request-btn"`

**components/FindingDriverModal.tsx:**
- Read the full file to find the Cancel and Try Again buttons, then add:
  - Cancel button: add `testID="cancel-search-btn"`
  - Try Again button: add `testID="try-again-btn"`

**components/ActiveTripSheet.tsx:**
- The action button TouchableOpacity (line ~230, the one with `ACTION_LABELS[trip?.status]`): add `testID="advance-trip-btn"`

**components/RequestPopup.tsx:**
- Accept button TouchableOpacity (line ~217): add `testID="accept-request-btn"`
- Decline button TouchableOpacity (line ~225): add `testID="decline-request-btn"`

IMPORTANT: Only add `testID` props. Do NOT change any other code, styling, or behavior. Each `testID` goes on the component's opening tag as a prop.
  </action>
  <verify>
    <automated>cd /Users/chris/projects/towlink && grep -c 'testID=' app/\(auth\)/login.tsx app/\(auth\)/signup.tsx app/\(commuter\)/index.tsx app/\(driver\)/index.tsx components/RequestServiceSheet.tsx components/FindingDriverModal.tsx components/ActiveTripSheet.tsx components/RequestPopup.tsx && echo "---" && grep -l 'testID="email-input"' app/\(auth\)/login.tsx && grep -l 'testID="submit-request-btn"' components/RequestServiceSheet.tsx && grep -l 'testID="advance-trip-btn"' components/ActiveTripSheet.tsx && grep -l 'testID="accept-request-btn"' components/RequestPopup.tsx && echo "ALL TESTID CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - "app/(auth)/login.tsx" contains `testID="email-input"`, `testID="password-input"`, `testID="login-btn"`
    - "app/(auth)/signup.tsx" contains `testID="signup-email-input"`, `testID="signup-password-input"`, `testID="signup-confirm-password-input"`, `testID="signup-btn"`
    - "app/(commuter)/index.tsx" contains `testID="request-assistance-btn"`
    - "app/(driver)/index.tsx" contains `testID="go-online-btn"` and `testID="online-toggle"`
    - components/RequestServiceSheet.tsx contains `testID="pickup-address-input"`, `testID="dropoff-address-input"`, `testID="vehicle-year-input"`, `testID="vehicle-make-input"`, `testID="vehicle-model-input"`, `testID="submit-request-btn"`, `testID="detect-location-btn"`
    - components/FindingDriverModal.tsx contains `testID="cancel-search-btn"` and `testID="try-again-btn"`
    - components/ActiveTripSheet.tsx contains `testID="advance-trip-btn"`
    - components/RequestPopup.tsx contains `testID="accept-request-btn"` and `testID="decline-request-btn"`
    - `npm run lint` passes without errors
  </acceptance_criteria>
  <done>All interactive elements used by Maestro flows have testID props for reliable element selection</done>
</task>

<task type="auto">
  <name>Task 2: Create Maestro YAML test flows and single-command runner script</name>
  <files>
    .maestro/subflows/login-commuter.yaml,
    .maestro/subflows/login-driver.yaml,
    .maestro/subflows/login-admin.yaml,
    .maestro/commuter-happy-path.yaml,
    .maestro/commuter-no-drivers.yaml,
    .maestro/driver-accept-complete.yaml,
    .maestro/driver-decline.yaml,
    .maestro/admin-basic.yaml,
    scripts/run-e2e.sh
  </files>
  <read_first>
    - "app/(auth)/login.tsx" (verify testIDs from Task 1)
    - "app/(commuter)/index.tsx"
    - "app/(driver)/index.tsx"
    - "app/(admin)/index.tsx"
    - app/(admin)/_layout.tsx
    - components/RequestServiceSheet.tsx
    - components/FindingDriverModal.tsx
    - components/ActiveTripSheet.tsx
    - components/RequestPopup.tsx
    - scripts/seed-emulator.js (verify test credentials)
    - .planning/phases/03-maestro-e2e-testing/03-RESEARCH.md
  </read_first>
  <action>
Create `.maestro/` directory structure and all YAML test flows. All flows use `appId: com.towlink.app` (matching the bundleIdentifier set in Plan 01).

**1. Create `.maestro/subflows/login-commuter.yaml`:**
```yaml
appId: com.towlink.app
--- # maestro-separator
- launchApp:
    clearState: true
- tapOn:
    id: "email-input"
- inputText: "test-commuter@test.com"
- tapOn:
    id: "password-input"
- inputText: "password123"
- hideKeyboard
- tapOn:
    id: "login-btn"
- assertVisible:
    text: "Commuter Screen"
    timeout: 10000
```

**2. Create `.maestro/subflows/login-driver.yaml`:**
```yaml
appId: com.towlink.app
--- # maestro-separator
- launchApp:
    clearState: true
- tapOn:
    id: "email-input"
- inputText: "test-driver@test.com"
- tapOn:
    id: "password-input"
- inputText: "password123"
- hideKeyboard
- tapOn:
    id: "login-btn"
- assertVisible:
    text: "Offline"
    timeout: 10000
```

**3. Create `.maestro/subflows/login-admin.yaml`:**
```yaml
appId: com.towlink.app
--- # maestro-separator
- launchApp:
    clearState: true
- tapOn:
    id: "email-input"
- inputText: "test-admin@test.com"
- tapOn:
    id: "password-input"
- inputText: "password123"
- hideKeyboard
- tapOn:
    id: "login-btn"
- assertVisible:
    text: "Jobs"
    timeout: 10000
```

**4. Create `.maestro/commuter-happy-path.yaml`** (per D-06):
```yaml
appId: com.towlink.app
--- # maestro-separator
# Commuter logs in and submits a tow request
- runFlow: subflows/login-commuter.yaml

# Tap "Request Roadside Assistance" button
- tapOn:
    id: "request-assistance-btn"

# Fill out the request form
- tapOn:
    id: "pickup-address-input"
- inputText: "123 Main St, Los Angeles, CA"

- tapOn:
    id: "dropoff-address-input"
- inputText: "456 Oak Ave, Los Angeles, CA"
- hideKeyboard

- tapOn:
    id: "vehicle-year-input"
- inputText: "2020"

- tapOn:
    id: "vehicle-make-input"
- inputText: "Toyota"

- tapOn:
    id: "vehicle-model-input"
- inputText: "Camry"
- hideKeyboard

# Scroll down to find submit button and tap
- scrollUntilVisible:
    element:
      id: "submit-request-btn"
    direction: DOWN
- tapOn:
    id: "submit-request-btn"

# Verify the finding driver modal appears
- assertVisible:
    text: "Searching"
    timeout: 10000
```
NOTE: The commuter happy-path cannot fully verify "driver assigned" without a real dispatch cycle against emulators. The test verifies the commuter can submit a request and sees the searching state. The Cloud Functions emulator would need to process the dispatch. If the functions are running and seed data is correct, the test may see "Driver on the way" but we set a reasonable timeout. The primary assertion is that the form submission works and the searching modal appears.

**5. Create `.maestro/commuter-no-drivers.yaml`** (per D-06 error case):
```yaml
appId: com.towlink.app
--- # maestro-separator
# This test verifies the no_drivers scenario.
# To trigger no_drivers, we need no available drivers in emulator.
# The seed script creates available drivers, so this test would need
# a modified seed or to run after drivers have been marked unavailable.
# For now, this is a placeholder that verifies the UI can handle the
# no_drivers state -- it logs in as commuter and submits a request
# to a location outside any company's service radius.

- runFlow: subflows/login-commuter.yaml

- tapOn:
    id: "request-assistance-btn"

# Use a location far from the seeded company (which is in LA)
- tapOn:
    id: "pickup-address-input"
- inputText: "123 Main St, Anchorage, AK"

- tapOn:
    id: "dropoff-address-input"
- inputText: "456 Oak Ave, Anchorage, AK"
- hideKeyboard

- tapOn:
    id: "vehicle-year-input"
- inputText: "2020"

- tapOn:
    id: "vehicle-make-input"
- inputText: "Honda"

- tapOn:
    id: "vehicle-model-input"
- inputText: "Civic"
- hideKeyboard

- scrollUntilVisible:
    element:
      id: "submit-request-btn"
    direction: DOWN
- tapOn:
    id: "submit-request-btn"

# Should eventually show no drivers available
# Using 60s timeout to account for Cloud Functions emulator cold-start latency (15-25s)
- assertVisible:
    text: "No drivers available"
    timeout: 60000

# Verify Try Again button appears
- assertVisible:
    id: "try-again-btn"
    timeout: 5000
```

**6. Create `.maestro/driver-accept-complete.yaml`** (per D-07, D-09):
```yaml
appId: com.towlink.app
--- # maestro-separator
# Driver logs in and sees the pre-seeded claimed request.
# The seed script (Plan 01) creates a request with:
#   status: 'claimed'
#   claimedByDriverId: <test-driver-uid>
# The driver's useClaimedRequest hook listens for exactly these fields
# via listenForClaimedRequests (queries status=='claimed' + claimedByDriverId==driverId).
# When the driver goes online, the hook picks up this request and shows RequestPopup.

- runFlow: subflows/login-driver.yaml

- tapOn:
    id: "go-online-btn"

# Dismiss the "You are now online!" alert
- tapOn:
    text: "OK"
    timeout: 5000

# The request popup should appear with the seeded job
- assertVisible:
    text: "New Request"
    timeout: 15000

# Accept the request
- tapOn:
    id: "accept-request-btn"

# Should see active trip sheet with "En Route to Pickup"
- assertVisible:
    text: "En Route to Pickup"
    timeout: 10000

# Advance through trip stages
- tapOn:
    id: "advance-trip-btn"
- assertVisible:
    text: "Arrived at Pickup"
    timeout: 5000

- tapOn:
    id: "advance-trip-btn"
- assertVisible:
    text: "Service in Progress"
    timeout: 5000

- tapOn:
    id: "advance-trip-btn"
- assertVisible:
    text: "Trip Completed"
    timeout: 5000
```

**7. Create `.maestro/driver-decline.yaml`** (per D-07 decline scenario):
```yaml
appId: com.towlink.app
--- # maestro-separator
# Driver logs in and declines the pre-seeded claimed request.
# Seed creates request with status:'claimed' + claimedByDriverId:<test-driver-uid>.
# This verifies the decline button works and the popup dismisses.

- runFlow: subflows/login-driver.yaml

- tapOn:
    id: "go-online-btn"

# Dismiss alert
- tapOn:
    text: "OK"
    timeout: 5000

# Wait for request popup
- assertVisible:
    text: "New Request"
    timeout: 15000

# Decline the request
- tapOn:
    id: "decline-request-btn"

# Should see "Request Declined" alert
- assertVisible:
    text: "Request Declined"
    timeout: 5000
- tapOn:
    text: "OK"
    timeout: 3000

# Driver should return to waiting state
- assertVisible:
    text: "Waiting for requests"
    timeout: 5000
```

**8. Create `.maestro/admin-basic.yaml`** (per D-08):
```yaml
appId: com.towlink.app
--- # maestro-separator
# Admin logs in and navigates between Jobs and Drivers tabs

- runFlow: subflows/login-admin.yaml

# Should see Jobs tab content (header says "Jobs")
- assertVisible:
    text: "Jobs"

# Tap Drivers tab
- tapOn:
    text: "Drivers"

# Should see drivers content (the seeded drivers should appear)
- assertVisible:
    text: "Drivers"
    timeout: 5000

# Tap back to Jobs tab
- tapOn:
    text: "Jobs"

- assertVisible:
    text: "Jobs"
    timeout: 5000
```

**9. Create `scripts/run-e2e.sh`** (per D-14 single command):
```bash
#!/bin/bash
set -e

echo "=== TowLink E2E Test Runner ==="
echo ""

# Check if emulators are running
if ! curl -s -o /dev/null http://localhost:8080/; then
  echo "ERROR: Firebase emulators are not running."
  echo "Start them in a separate terminal: npm run emulators"
  exit 1
fi

echo "1. Clearing and re-seeding emulator data..."
node scripts/seed-emulator.js

echo ""
echo "2. Granting location permission to app on iOS Simulator..."
xcrun simctl privacy booted grant location com.towlink.app 2>/dev/null || echo "   (skipped -- no booted simulator or permission already granted)"

echo ""
echo "3. Running Maestro tests..."
echo ""

# Run tests -- pass any arguments through (e.g., specific flow file)
if [ "$1" ]; then
  maestro test "$@"
else
  maestro test .maestro/
fi

echo ""
echo "=== E2E Tests Complete ==="
```
Then run `chmod +x scripts/run-e2e.sh`.

**10. Update package.json** -- Change the `test:e2e` script to use the shell script:
```json
"test:e2e": "./scripts/run-e2e.sh"
```
Keep `test:e2e:run`, `emulators`, and `emulators:seed` scripts from Plan 01.

IMPORTANT NOTES:
- All YAML files use `appId: com.towlink.app` matching the bundleIdentifier from Plan 01
- Login subflows use `launchApp: clearState: true` to ensure a fresh app state
- The driver tests depend on the seed script creating a request with `status: 'claimed'` and `claimedByDriverId` -- this is confirmed correct and matches the `listenForClaimedRequests` hook contract in `services/firebase/firestore.ts`
- The `xcrun simctl privacy booted grant location com.towlink.app` command pre-grants location permission to avoid the system dialog blocking Maestro
  </action>
  <verify>
    <automated>cd /Users/chris/projects/towlink && ls .maestro/subflows/login-commuter.yaml .maestro/subflows/login-driver.yaml .maestro/subflows/login-admin.yaml .maestro/commuter-happy-path.yaml .maestro/commuter-no-drivers.yaml .maestro/driver-accept-complete.yaml .maestro/driver-decline.yaml .maestro/admin-basic.yaml scripts/run-e2e.sh && grep -q "com.towlink.app" .maestro/admin-basic.yaml && grep -q "test-commuter@test.com" .maestro/subflows/login-commuter.yaml && grep -q "test-driver@test.com" .maestro/subflows/login-driver.yaml && grep -q "test-admin@test.com" .maestro/subflows/login-admin.yaml && test -x scripts/run-e2e.sh && echo "ALL MAESTRO FILE CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - .maestro/subflows/login-commuter.yaml exists and contains `test-commuter@test.com` and `appId: com.towlink.app`
    - .maestro/subflows/login-driver.yaml exists and contains `test-driver@test.com` and `appId: com.towlink.app`
    - .maestro/subflows/login-admin.yaml exists and contains `test-admin@test.com` and `appId: com.towlink.app`
    - .maestro/commuter-happy-path.yaml exists and contains `runFlow: subflows/login-commuter.yaml` and `id: "submit-request-btn"`
    - .maestro/commuter-no-drivers.yaml exists with `timeout: 60000` on "No drivers available" assertion
    - .maestro/driver-accept-complete.yaml exists and contains `runFlow: subflows/login-driver.yaml` and `id: "advance-trip-btn"`
    - .maestro/driver-decline.yaml exists and contains `id: "decline-request-btn"`
    - .maestro/admin-basic.yaml exists and contains `runFlow: subflows/login-admin.yaml`
    - scripts/run-e2e.sh exists and is executable (`test -x`)
    - scripts/run-e2e.sh contains `maestro test`, `seed-emulator.js`, and `xcrun simctl privacy`
    - `npm run lint` passes without errors
  </acceptance_criteria>
  <done>All 5 Maestro test flows + 3 login subflows created with correct testID selectors and seed data credentials, plus executable single-command runner script</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Build dev build and verify E2E test suite</name>
  <what-built>
    Complete Maestro E2E testing infrastructure:
    - Firebase emulators with Auth+Firestore+Functions
    - Seed script with deterministic test data (using status:'claimed' + claimedByDriverId to match hook contract)
    - Expo dev build with expo-dev-client
    - testID props on all interactive screen elements
    - 5 Maestro test flows (commuter happy path, commuter no-drivers, driver accept, driver decline, admin basic)
    - 3 reusable login subflows
    - Single-command test runner script
  </what-built>
  <how-to-verify>
    1. **Build the Expo dev build** (one-time, takes ~5 minutes):
       ```bash
       # Load the test env vars and build
       cp .env.test .env.test.bak  # backup
       npx expo run:ios
       ```
       Wait for the build to complete and the app to launch in the iOS Simulator.

    2. **Start Firebase emulators** (in a separate terminal):
       ```bash
       cd /Users/chris/projects/towlink
       npm run emulators
       ```
       Verify emulator UI at http://localhost:4000 shows Auth, Firestore, Functions tabs.

    3. **Run the full E2E test suite**:
       ```bash
       npm run test:e2e
       ```
       This will seed the emulators and run all Maestro flows.

    4. **Expected results**:
       - `admin-basic.yaml`: PASS -- admin logs in, sees Jobs tab, taps Drivers tab, taps back
       - `commuter-happy-path.yaml`: PASS -- commuter logs in, fills form, submits, sees "Searching"
       - `driver-accept-complete.yaml`: PASS -- seed data uses correct status:'claimed' + claimedByDriverId matching the listenForClaimedRequests hook
       - `driver-decline.yaml`: PASS -- same seed data contract
       - `commuter-no-drivers.yaml`: PASS if Cloud Functions process the out-of-area request correctly (60s timeout for cold-start)

    5. **If any flow fails**: Note which step fails and describe what you see. Common fixes:
       - Element not found: testID may be missing or wrong
       - Timeout: Emulators may be slow, increase timeout
       - Request popup not appearing: Verify seed script created request with status:'claimed' and claimedByDriverId (check Firestore emulator UI at localhost:4000)
  </how-to-verify>
  <resume-signal>Type "approved" if at least admin-basic and commuter-happy-path pass, or describe issues for each failing flow</resume-signal>
</task>

</tasks>

<verification>
1. All 8 Maestro YAML files exist in `.maestro/` with correct `appId: com.towlink.app`
2. All screen files have testID props on interactive elements
3. `scripts/run-e2e.sh` is executable and contains seed + maestro commands
4. `npm run test:e2e` executes the full suite (after dev build + emulators)
5. `npm run lint` passes
6. At minimum, admin-basic and commuter-happy-path flows pass
</verification>

<success_criteria>
- Maestro is installed and configured to run against the Expo development build (SC-1)
- An automated test flow covers the commuter request submission flow end-to-end (SC-2)
- An automated test flow covers the driver receiving, accepting, and completing a trip (SC-3)
- Tests can be run with a single command and produce pass/fail results (SC-4)
</success_criteria>

<output>
After completion, create `.planning/phases/03-maestro-e2e-testing/03-02-SUMMARY.md`
</output>
