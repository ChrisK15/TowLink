---
phase: 06-security-reliability-testing
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - app/(commuter)/index.tsx
  - app/(driver)/index.tsx
  - app/(admin)/drivers.tsx
  - app/(admin)/index.tsx
  - app/(admin)/company-setup.tsx
  - app/(auth)/index.tsx
  - components/FindingDriverModal.tsx
  - components/ActiveTripSheet.tsx
  - components/RequestServiceSheet.tsx
  - components/CommuterTripSheet.tsx
autonomous: true
requirements: [SEC-02, SEC-03]

must_haves:
  truths:
    - "Error Alert.alert calls in screens are replaced with Toast.show({ type: 'error' })"
    - "Confirmation dialogs (sign out, cancel job) remain as Alert.alert — only error alerts migrate"
    - "Screens with async Firebase ops use LoadingOverlay instead of inline ActivityIndicator"
    - "MapView in commuter and driver screens is wrapped with MapErrorBoundary"
  artifacts:
    - path: "app/(commuter)/index.tsx"
      provides: "Commuter screen with Toast errors and MapErrorBoundary"
      contains: "Toast.show"
    - path: "app/(driver)/index.tsx"
      provides: "Driver screen with Toast errors and MapErrorBoundary"
      contains: "Toast.show"
    - path: "components/ActiveTripSheet.tsx"
      provides: "ActiveTripSheet with Toast error notifications"
      contains: "Toast.show"
  key_links:
    - from: "app/(commuter)/index.tsx"
      to: "react-native-toast-message"
      via: "Toast.show() replacing Alert.alert in catch blocks"
      pattern: "Toast\\.show"
    - from: "app/(commuter)/index.tsx"
      to: "components/ErrorBoundary.tsx"
      via: "MapErrorBoundary wrapping MapView"
      pattern: "<MapErrorBoundary>"
    - from: "app/(admin)/drivers.tsx"
      to: "components/LoadingOverlay.tsx"
      via: "LoadingOverlay replacing inline ActivityIndicator"
      pattern: "<LoadingOverlay"
---

<objective>
Migrate all screens from Alert.alert error pattern to Toast.show, replace inline ActivityIndicator with LoadingOverlay, and wrap MapView instances with MapErrorBoundary.

Purpose: SEC-02 requires consistent loading states. SEC-03 requires user-friendly error messages via toasts. This plan completes the screen-level migration using components created in Plan 02.
Output: 10 modified screen/component files with consistent error and loading UX
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-security-reliability-testing/06-CONTEXT.md
@.planning/phases/06-security-reliability-testing/06-RESEARCH.md
@.planning/phases/06-security-reliability-testing/06-UI-SPEC.md
@.planning/phases/06-security-reliability-testing/06-02-SUMMARY.md

<interfaces>
<!-- Components created in Plan 02 -->
From components/LoadingOverlay.tsx:
```typescript
interface LoadingOverlayProps {
  visible: boolean;
  message?: string;
}
export function LoadingOverlay({ visible, message }: LoadingOverlayProps): JSX.Element;
```

From components/ErrorBoundary.tsx:
```typescript
export class ErrorBoundary extends Component<{ children: ReactNode; fallback?: ReactNode }, { hasError: boolean }>;
export class MapErrorBoundary extends Component<{ children: ReactNode; fallback?: ReactNode }, { hasError: boolean }>;
```

From react-native-toast-message:
```typescript
import Toast from 'react-native-toast-message';
Toast.show({ type: 'error' | 'success', text1: string, text2?: string, visibilityTime?: number });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Alert.alert errors to Toast.show in screens</name>
  <files>
    app/(commuter)/index.tsx,
    app/(driver)/index.tsx,
    app/(admin)/drivers.tsx,
    components/FindingDriverModal.tsx,
    components/ActiveTripSheet.tsx,
    components/RequestServiceSheet.tsx,
    components/CommuterTripSheet.tsx
  </files>
  <read_first>
    - app/(commuter)/index.tsx (find all Alert.alert calls — distinguish error alerts from confirmation dialogs)
    - app/(driver)/index.tsx (find all Alert.alert calls — distinguish error alerts from confirmation/info alerts)
    - app/(admin)/drivers.tsx (find Alert.alert calls)
    - components/FindingDriverModal.tsx (find Alert.alert calls)
    - components/ActiveTripSheet.tsx (find Alert.alert calls — note the Cancel Job confirmation dialog at line ~136 must STAY as Alert.alert)
    - components/RequestServiceSheet.tsx (find Alert.alert calls)
    - components/CommuterTripSheet.tsx (find Alert.alert calls — note the Cancel Trip confirmation dialog at line ~149 must STAY as Alert.alert)
    - .planning/phases/06-security-reliability-testing/06-UI-SPEC.md (Copywriting Contract for toast messages)
  </read_first>
  <action>
For each file, add `import Toast from 'react-native-toast-message';` at the top. Then replace Alert.alert ERROR calls (not confirmation dialogs) with Toast.show. Keep `Alert` import if the file still uses Alert.alert for confirmation dialogs.

**CRITICAL RULE: Confirmation dialogs with multiple buttons (Cancel/OK) MUST remain as Alert.alert.** Only single-button error/info alerts migrate to Toast.show.

**Per-file migration (exact replacements):**

### app/(commuter)/index.tsx
- Line ~123 `Alert.alert('Request Cancelled'...)` — Toast: `Toast.show({ type: 'info', text1: 'Request cancelled', text2: 'Please try again.', visibilityTime: 3000 })`
- Line ~141 `Alert.alert(String(error))` — Toast: `Toast.show({ type: 'error', text1: 'Could not cancel your request', text2: 'Please try again.', visibilityTime: 3000 })`
- Line ~161 `Alert.alert('Location required...')` — Toast: `Toast.show({ type: 'error', text1: 'Location required', text2: 'Please wait for your location to load.', visibilityTime: 3000 })`
- Line ~166 `Alert.alert('GPS Not Ready'...)` — Toast: `Toast.show({ type: 'error', text1: 'GPS not ready', text2: 'Waiting for GPS signal...', visibilityTime: 3000 })`
- Line ~288 `Alert.alert('Sign Out'...)` — KEEP as Alert.alert (confirmation dialog with Cancel/OK buttons)

### app/(driver)/index.tsx
- Line ~258 `Alert.alert('Error', 'You must be signed in')` — Toast: `Toast.show({ type: 'error', text1: 'You must be signed in', visibilityTime: 3000 })`
- Line ~262 `Alert.alert('Location Required'...)` — Toast: `Toast.show({ type: 'error', text1: 'Location required', text2: 'Please enable location to go online.', visibilityTime: 3000 })`
- Line ~275 `Alert.alert(value ? 'You are now online!' : 'You are now offline!')` — Toast: `Toast.show({ type: 'success', text1: value ? 'You are now online' : 'You are now offline', visibilityTime: 2000 })`
- Line ~277 `Alert.alert(...)` error in availability toggle — Toast: `Toast.show({ type: 'error', text1: 'Could not update availability', text2: 'Please try again.', visibilityTime: 3000 })`
- Line ~306 `Alert.alert(...)` — check context; if error alert, replace with Toast
- Line ~324 `Alert.alert(String(error))` — Toast: `Toast.show({ type: 'error', text1: 'Something went wrong', text2: 'Please try again.', visibilityTime: 3000 })`
- Line ~356 `Alert.alert('Error', error.message)` — Toast: `Toast.show({ type: 'error', text1: 'Could not complete action', text2: error instanceof Error ? error.message : String(error), visibilityTime: 3000 })`
- Line ~369 `Alert.alert('Request Declined'...)` — Toast: `Toast.show({ type: 'info', text1: 'Request declined', text2: 'Looking for another driver...', visibilityTime: 3000 })`
- Line ~371 `Alert.alert('Error', error.message)` — Toast with same pattern as line 356
- Line ~472 `Alert.alert('Sign Out'...)` — KEEP as Alert.alert (confirmation dialog)

### app/(admin)/drivers.tsx
- Line ~126 `Alert.alert(...)` — check context: if confirmation for deactivate, KEEP; if error, replace with Toast
- Line ~138 `Alert.alert('Error', e.message)` — Toast: `Toast.show({ type: 'error', text1: 'Could not update driver', text2: e instanceof Error ? e.message : String(e), visibilityTime: 3000 })`

### components/FindingDriverModal.tsx
- Line ~103 `Alert.alert(...)` — Toast: `Toast.show({ type: 'error', text1: 'Could not cancel request', text2: 'Please try again.', visibilityTime: 3000 })`
- Line ~114 `Alert.alert(...)` — same toast pattern for cancel error

### components/ActiveTripSheet.tsx
- Line ~104 `Alert.alert('Error', 'Could not open phone dialer')` — Toast: `Toast.show({ type: 'error', text1: 'Could not open phone dialer', visibilityTime: 3000 })`
- Line ~111 `Alert.alert('Error', 'Could not open messages')` — Toast: `Toast.show({ type: 'error', text1: 'Could not open messages', visibilityTime: 3000 })`
- Line ~130 `Alert.alert('Error', error.message)` — Toast: `Toast.show({ type: 'error', text1: 'Action failed', text2: error instanceof Error ? error.message : String(error), visibilityTime: 3000 })`
- Line ~136 `Alert.alert(...)` Cancel Job confirmation — KEEP as Alert.alert (D-06 note: confirmation dialogs remain as Alert.alert)
- Line ~153 `Alert.alert('Error', error.message)` — Toast with same pattern

### components/RequestServiceSheet.tsx
- Line ~154 `Alert.alert(...)` — Toast: check context, replace error alerts only
- Line ~176 `Alert.alert(...)` — Toast pattern for validation/error
- Line ~211 `Alert.alert(...)` — Toast for validation
- Line ~224 `Alert.alert(...)` — Toast for validation
- Line ~264 `Alert.alert('Error', 'Failed to submit request...')` — Toast: `Toast.show({ type: 'error', text1: 'Failed to submit request', text2: 'Please try again.', visibilityTime: 3000 })`

### components/CommuterTripSheet.tsx
- Line ~137 `Alert.alert('Error', 'Could not open phone dialer')` — Toast: `Toast.show({ type: 'error', text1: 'Could not open phone dialer', visibilityTime: 3000 })`
- Line ~144 `Alert.alert('Error', 'Could not open messages')` — Toast: `Toast.show({ type: 'error', text1: 'Could not open messages', visibilityTime: 3000 })`
- Line ~149 `Alert.alert('Cancel Trip'...)` — KEEP as Alert.alert (confirmation dialog)
- Line ~158 `Alert.alert('Error', error.message)` — Toast: `Toast.show({ type: 'error', text1: 'Could not cancel trip', text2: error instanceof Error ? error.message : String(error), visibilityTime: 3000 })`

**Toast message rules per UI-SPEC Copywriting Contract:**
- `text1` names the specific operation that failed (never generic "Error occurred")
- `text2` provides next step ("try again", "check connection")
- `visibilityTime: 3000` on all toasts
- No "Oops" or exclamation marks in error states
- `type: 'error'` for failures, `type: 'success'` for confirmations, `type: 'info'` for status updates

Remove `Alert` import from files that no longer use it. Keep `Alert` import in files that retain confirmation dialogs.
  </action>
  <verify>
    <automated>cd /Users/chris/projects/towlink && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/(commuter)/index.tsx contains `import Toast from 'react-native-toast-message'`
    - app/(commuter)/index.tsx contains `Toast.show` calls with `visibilityTime: 3000`
    - app/(commuter)/index.tsx still contains `Alert.alert('Sign Out'` (confirmation dialog preserved)
    - app/(driver)/index.tsx contains `Toast.show` calls
    - app/(driver)/index.tsx still contains `Alert.alert('Sign Out'` (confirmation dialog preserved)
    - components/ActiveTripSheet.tsx contains `Toast.show` calls
    - components/ActiveTripSheet.tsx still contains `Alert.alert` for Cancel Job confirmation
    - components/CommuterTripSheet.tsx still contains `Alert.alert` for Cancel Trip confirmation
    - components/FindingDriverModal.tsx contains `Toast.show` and does NOT contain `Alert.alert`
    - components/RequestServiceSheet.tsx contains `Toast.show`
    - No file has error-only Alert.alert remaining (all migrated to Toast)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>All error Alert.alert calls migrated to Toast.show; confirmation dialogs preserved as Alert.alert</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline ActivityIndicator with LoadingOverlay and wrap MapViews with MapErrorBoundary</name>
  <files>
    app/(auth)/index.tsx,
    app/(admin)/index.tsx,
    app/(admin)/drivers.tsx,
    app/(admin)/company-setup.tsx,
    app/(commuter)/index.tsx,
    app/(driver)/index.tsx
  </files>
  <read_first>
    - app/(auth)/index.tsx (find ActivityIndicator usage for loading guard)
    - app/(admin)/index.tsx (find ActivityIndicator usage for loading guard)
    - app/(admin)/drivers.tsx (find ActivityIndicator usage during driver fetch)
    - app/(admin)/company-setup.tsx (find ActivityIndicator usage during save)
    - app/(commuter)/index.tsx (find MapView component to wrap with MapErrorBoundary)
    - app/(driver)/index.tsx (find MapView component to wrap with MapErrorBoundary)
  </read_first>
  <action>
**Part A: LoadingOverlay migration**

For each screen that has an inline `<ActivityIndicator>` as a loading guard, replace it with `<LoadingOverlay visible={loading} />`:

1. `app/(auth)/index.tsx`:
   - Add `import { LoadingOverlay } from '@/components/LoadingOverlay';`
   - Find the `if (loading) return <ActivityIndicator ... />` pattern
   - Replace with: render the normal screen content AND `<LoadingOverlay visible={loading} />` at the bottom of the return JSX. This way the overlay appears above the content rather than replacing it with a bare spinner.
   - If the screen truly needs to block all content during initial load (e.g., auth redirect check), keep the early return but return `<LoadingOverlay visible={true} />` instead of bare `<ActivityIndicator>`.

2. `app/(admin)/index.tsx`:
   - Same pattern: replace `ActivityIndicator` loading guard with `<LoadingOverlay visible={authLoading || loading} />`
   - The overlay should appear above the normal screen content.

3. `app/(admin)/drivers.tsx`:
   - Replace the inline `ActivityIndicator` during driver list fetch with `<LoadingOverlay visible={loading} />`

4. `app/(admin)/company-setup.tsx`:
   - Replace any `ActivityIndicator` during save with `<LoadingOverlay visible={saving} />` (check the actual state variable name)

For ALL LoadingOverlay replacements: remove the `ActivityIndicator` import from `react-native` if no longer used in that file.

**Part B: MapErrorBoundary wrapping**

5. `app/(commuter)/index.tsx`:
   - Add `import { MapErrorBoundary } from '@/components/ErrorBoundary';`
   - Find the `<MapView ... />` JSX element
   - Wrap it: `<MapErrorBoundary><MapView ... /></MapErrorBoundary>`
   - The MapErrorBoundary catches Google Maps SDK render crashes and shows "Map unavailable" fallback

6. `app/(driver)/index.tsx`:
   - Same pattern: wrap `<MapView ... />` with `<MapErrorBoundary>`
  </action>
  <verify>
    <automated>cd /Users/chris/projects/towlink && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/(auth)/index.tsx contains `import { LoadingOverlay }` and `<LoadingOverlay`
    - app/(auth)/index.tsx does NOT contain a bare `<ActivityIndicator` as a full-screen loading guard (may still use ActivityIndicator for inline spinners if any)
    - app/(admin)/index.tsx contains `<LoadingOverlay`
    - app/(admin)/drivers.tsx contains `<LoadingOverlay`
    - app/(admin)/company-setup.tsx contains `<LoadingOverlay`
    - app/(commuter)/index.tsx contains `<MapErrorBoundary>` wrapping `<MapView`
    - app/(driver)/index.tsx contains `<MapErrorBoundary>` wrapping `<MapView`
    - app/(commuter)/index.tsx contains `import { MapErrorBoundary } from '@/components/ErrorBoundary'`
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>All screens use LoadingOverlay for async operations; MapView wrapped with MapErrorBoundary on commuter and driver screens</done>
</task>

</tasks>

<verification>
- `npm run lint` — no lint errors across all modified files
- `npx expo start` — app launches, screens display without crashes
- Trigger an error condition (e.g., disconnect network) to verify toast appears instead of Alert.alert
- Verify MapView loads normally inside MapErrorBoundary (no regressions)
</verification>

<success_criteria>
- All error Alert.alert calls across 7 screen/component files replaced with Toast.show (SEC-03)
- Confirmation dialogs (Sign Out, Cancel Job, Cancel Trip) remain as Alert.alert
- 4 admin/auth screens use LoadingOverlay instead of inline ActivityIndicator (SEC-02)
- MapView on commuter and driver screens wrapped with MapErrorBoundary (D-07)
- All toast messages follow the Copywriting Contract (specific operation name, next step, no exclamation marks)
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-reliability-testing/06-03-SUMMARY.md`
</output>
